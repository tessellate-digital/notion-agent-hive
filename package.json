{
  "name": "notion-agent-hive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:claude": "tsx scripts/generate-claude-agents.ts",
    "generate:opencode": "tsx scripts/generate-opencode-agents.ts",
    "generate": "npm run generate:claude && npm run generate:opencode",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^3.1.0"
  }
}
